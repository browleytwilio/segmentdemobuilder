<Warning>
  **Credentials are never persisted to the database.** API keys and tokens are held in browser memory only during your session. The database stores only sanitized placeholders (`YOUR_SEGMENT_WRITE_KEY`, etc.). If you close the browser or clear state, re-inject your credentials via the [Rehydration Modal](/tutorials/rehydrate-playbook).
</Warning>
